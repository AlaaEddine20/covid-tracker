import { Paper } from "@material-ui/core";
import React from "react";
import styles from "./Styles.module.scss";

const Charts = () => {
  return (
    <Paper elevation={3} className={styles.charts_container}>
      <h2>Charts</h2>
    </Paper>
  );
};

export default Charts;
